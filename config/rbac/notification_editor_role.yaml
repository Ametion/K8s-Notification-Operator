# This rule is not used by the project k8s-notification-operator itself.
# It is provided to allow the cluster admin to help manage permissions for users.
#
# Grants permissions to create, update, and delete resources within the notification.res-x.com.
# This role is intended for users who need to manage these resources
# but should not control RBAC or manage permissions for others.

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: k8s-notification-operator
    app.kubernetes.io/managed-by: kustomize
  name: notification-editor-role
rules:
- apiGroups:
  - notification.res-x.com
  resources:
  - notifications
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - notification.res-x.com
  resources:
  - notifications/status
  verbs:
  - get
